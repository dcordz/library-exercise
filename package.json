{
  "name": "baltimore-city-interview",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx tsc --outdir dist --target es2022 --moduleResolution nodenext --module nodenext index.ts && node dist/index.js && rm -rf dist",
    "test:docker": "docker build . -t library:latest; docker run --rm library:latest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">= 20.12.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
